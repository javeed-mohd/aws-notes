Inode, symlink & hardlink:
==========================

inode = Stores metadata of the file/directory (folder)

file type -> file/directory/link
Owner group
Permissions
size
create, update, etc
actual memory block address

ls -li           --> Gives list files with inode numbers in long listing format of the file
stat <file-name> --> Gives entire information of the file

--> inode does not store actual file content

symlink/shortlink/softlink:
===========================

ln -s ../<file-name> <folder-name>

1. Short cut to the file/folder
2. inode number is different for symlink
3. If actual file is removed, then symlink will break
4. Uses for backward compatibility, switching versions and rollback is easy
5. You can create symlink for folders and files also
6. You can create symlink across different filesystem (xfs, ext3, ext4...)

YUM -> Before RHEL-8, YUM is the package manager
From RHEL-8, DNF is the package manager

ln --help
ln -sfn dnf-4 dnf

app-3
app-4

/var/lib/jenkins -> jenkins-2.8

jenkins-2.9

/var/lib/jenkins -> jenkins-2.9


Hardlink:
=========
Same file, two names

ln <file-name> <new file-name>

1. You can't create hardlink in another filesystem
2. Hardlink is same as original file but can be different name
3. Hardlink inode numbers are same
4. They points to same memory location, so memory is not wasted
5. You can't create hardlink to folders/directories
6. Hardlinks are used for backup.
7. When all links are deleted then only, file will be deleted.

Using inode number and find command within the same filesystem 

sudo find /home -xdev inum <inode-number>


Manual Process:
===============

1. Time taking
2. Human errors
3. Every time from the scratch
4. Can't rely
5. If scale is increased, manual way is not possible


Scripting:
==========
Commands in a file and run it, called scripting.


Where to store scripts?
=======================

git -> VCS (Version Control System)

1. Version control
2. Who did the changes, when did the changes
3. You can track who did the changes
4. Restoring is easy
5. Review is also easy -> Pull Requests
6. Secure place to store the code


Centralised vs Distributed:
===========================
SVN -> Sub Version Control

10 files
9 files 
staging/temp area -> add 9 files to staging area
then commit to local repo

1. clone the repo -> first time download

main/master is the default branch
.git is the difference between normal folder and git repo

normal folder --> plain file
repo folder   --> branch name highlight ex: main/master


#!/bin/bash -> SHEBANG

shebang is the interpreter of the code in shell script that checks and compiles for errors

Bash scripting == Shell scripting

2. add files to staging area

git add <file-name>

git rm --cached <file-name> --> remove from staging area

git config --global user.email "info@joindevops.com"
git config --global user.name "Sivakumar Reddy M"

3. commit to local repo

git commit -m "hello world completed"

git log --> For the first time user to show who has made changes in the git (Credential manager)

git push origin main

git pull

don't edit files inside LINUX SERVER...


Create a new repository on the command line:
============================================

echo "# shell-practice" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/javeed-mohd/shell-practice.git
git push -u origin main


Push an existing repository from the command line:
==================================================

git remote add origin https://github.com/javeed-mohd/shell-practice.git
git branch -M main
git push -u origin main

git clone https://github.com/javeed-mohd/shell-practice.git
git pull

rm -r shell-practice/ --> If cloned multiple times to remove them and we can clone again, no data will be lost.

git status --> Staging area (Green), Red --> It is in your workspace not in staging area

git rm --cached <file-name> --> remove from staging area


If we are already created a folder in the workspace then what we should do?

--> cd /c/devops/daws-88s/repos/ansible/ ==> Go to that folder (We have to create the same repo name in GitHub)
--> git init ==> Initializing empty git repository in C:/devops/daws-88s/repos/ansible/.git/
--> git branch -M main (From master to main conversion)
--> git remote add origin https://github.com/javeed-mohd/ansible.git ==> In this way, we need to add the folder into git if we created in our workspace....
--> git add . ; git commit -m "actions" ; git push origin main


******
If you don't want to delete the repo in git follow this:

git checkout main
Already on 'main'

DELL@JAVEED-LAPTOP MINGW64 /c/devops/daws-88s/repos/terraform-aws-vpc (main)
$ git pull origin main --rebase
From https://github.com/javeed-mohd/terraform-aws-vpc
 * branch            main       -> FETCH_HEAD
Already up to date.

DELL@JAVEED-LAPTOP MINGW64 /c/devops/daws-88s/repos/terraform-aws-vpc (main)
$ git push origin main
Everything up-to-date



==> If you want to rename the git repository name first we have to change in GitHub repo

Then, in the older repository folder we should provide,

==> git remote set-url origin https://github.com/javeed-mohd/k8s-resources.git [Renamed repository link]

If name did not change in local folder (computer), VS code we have rename it manually, first get one step back from that folder in gitbash and close the VS code first.

==> mv <older repo-name> <new repo-name> [Ex: mv k8-resources k8s-resources]

To check it successfully renamed, use:
==> git remote -v
origin  https://github.com/javeed-mohd/k8s-resources.git (fetch)
origin  https://github.com/javeed-mohd/k8s-resources.git (push)

--> git fetch
--> git status

==> We should get this as output for successful renaming...



==> If we want to clone the other person's repository to our local machine, then we have to:
--> Copy the repo URL from code of that person from their GitHub account.
--> Clone the repo in our gitbash by using git clone (URL)
--> cd repo (Moving to that repository) ==> Going to that folder (We have to create the same repo name in GitHub)

--> git remote remove origin   (Removing the origin of old owner as it still points to same origin if we do any changes)
--> git branch -M main (From master to main conversion)
--> git remote add origin https://github.com/javeed-mohd/terraform-aws-workstation.git ==> In this way, we need to add the folder into git if we created in our workspace....
--> git add . ; git commit -m "actions" ; git push origin main


Flow:
~~~~~

(gitbash) 
cd /c/devops/daws-88s/repos/

DELL@JAVEED-LAPTOP MINGW64 /c/devops/daws-88s/repos
$ git clone https://github.com/daws-88s/terraform-aws-workstation.git

DELL@JAVEED-LAPTOP MINGW64 /c/devops/daws-88s/repos
$ cd terraform-aws-workstation/

DELL@JAVEED-LAPTOP MINGW64 /c/devops/daws-88s/repos/terraform-aws-workstation (main)
$ git remote -v
origin  https://github.com/daws-88s/terraform-aws-workstation.git (fetch)
origin  https://github.com/daws-88s/terraform-aws-workstation.git (push)

DELL@JAVEED-LAPTOP MINGW64 /c/devops/daws-88s/repos/terraform-aws-workstation (main)
$ git remote remove origin

DELL@JAVEED-LAPTOP MINGW64 /c/devops/daws-88s/repos/terraform-aws-workstation (main)
$ git remote -v

DELL@JAVEED-LAPTOP MINGW64 /c/devops/daws-88s/repos/terraform-aws-workstation (main)
$ git branch -M main

DELL@JAVEED-LAPTOP MINGW64 /c/devops/daws-88s/repos/terraform-aws-workstation (main)
$ git remote add origin https://github.com/javeed-mohd/terraform-aws-workstation.git


==> If the other persons name should not appear where i got it from should do this,

DELL@JAVEED-LAPTOP MINGW64 /c/devops/daws-88s/repos/terraform-aws-workstation (main)
$ git checkout --orphan latest_branch
Switched to a new branch 'latest_branch'

DELL@JAVEED-LAPTOP MINGW64 /c/devops/daws-88s/repos/terraform-aws-workstation (latest_branch)
$ git branch -D main
Deleted branch main (was 0d57099).

DELL@JAVEED-LAPTOP MINGW64 /c/devops/daws-88s/repos/terraform-aws-workstation (latest_branch)
$ git branch -M main

DELL@JAVEED-LAPTOP MINGW64 /c/devops/daws-88s/repos/terraform-aws-workstation (main)
$ git push -f origin main

DELL@JAVEED-LAPTOP MINGW64 /c/devops/daws-88s/repos/terraform-aws-workstation (main)
$ git add . ; git commit -m "k8" ; git push origin main

DELL@JAVEED-LAPTOP MINGW64 /c/devops/daws-88s/repos/terraform-aws-workstation (main)
$ git log
commit db456092b68e4412d0d1f5c9ccebfd637fbeef01 (HEAD -> main, origin/main)
Author: Javeed Mohammed <javeedmohd214@gmail.com>
Date:   Thu Apr 9 13:35:59 2026 +0530

    k8


==> Should not use -f in real projects as other members work will be there...




(OR)
--> Copy the repo URL from code of that person from their GitHub account.
--> Clone the repo in our gitbash by using git clone (URL)
--> cd repo (Moving to that repository) ==> Going to that folder (We have to create the same repo name in GitHub)

--> git remote remove origin   (Removing the origin of old owner as it still points to same origin if we do any changes)
--> git remote set-url origin https://github.com/javeed-mohd/terraform-aws-workstation.git [Repository URL which we have created in github] 	==> In this way, we need to add the folder into git if we created in our workspace....
--> git add . ; git commit -m "actions" ; git push origin main
(OR)
We can directly download the zip file from the other person's GitHub repository that we want. 
Go to the repository on GitHub
Click the “Code” button
Select “Download ZIP”
Extract the ZIP file on your computer
--> After extracting, we can create the new repo in our GitHub

--> cd /c/devops/daws-88s/repos/terraform-aws-workstation/ ==> Go to that folder (We have to create the same repo name in GitHub)
--> git init ==> Initializing empty git repository in C:/devops/daws-88s/repos/terraform-aws-workstation/.git/
--> git branch -M main (From master to main conversion)
--> git remote add origin https://github.com/javeed-mohd/terraform-aws-workstation.git ==> In this way, we need to add the folder into git if we created in our workspace....
--> git add . ; git commit -m "actions" ; git push origin main