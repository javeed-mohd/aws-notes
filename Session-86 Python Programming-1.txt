python.org

--> Variables, Datatypes
--> Conditions
--> Loops
--> Functions

==> Object Oriented Programming (OOP)
==> Multi threading, multi processing
==> Working with 3rd party libraries

==> Python files should always end with ".py" extension, then only python compiler can understand the statements provided in the file.


Datatypes in python:
====================

--> Standard datatypes: integers, float, string, Boolean
--> Advanced datatypes: lists, tuples, dictionaries, sets


Variables:
==========

Variables are containers for storing data values.

Python has no command for declaring a variable.

A variable is created the moment you first assign a value to it.

--> Variables do not need to be declared with any particular type, and can even change type after they have been set.


Variable naming conventions:
============================
--> camelCase
--> snake_case
--> pascalcase


Rules for naming convention:
-----------------------------

--> It must start with a letter or the underscore character.
--> Shouldn't start with a number
--> Shouldn't have spaces between words
--> Can start with _ (underscore) but not with any other character such as "-:{}" etc., 
--> Choose a meaningful name for your variable (recommended) 
--> A variable name can only contain alpha-numeric characters and underscores (A-z, 0-9, and _ )
--> Variable names are case-sensitive (var, Var and VAR are three different variables)
--> A variable name cannot be any of the Python keywords.


Comments in python:
===================

==> Single line comments:
# This is my integer : Single line comment
my_int = 23

==> Inline comments:
Value 23 is stored in my_int container : Inline comment

==> Block comments / Multi-line comments:

'''
Comments in python
Single line comments
Inline comments
Block comments
'''

"""
Comments in python
Single line comments
Inline comments
Block comments
"""

--> Python is case sensitive
Ex: 
my_int and My_int are 2 different variables
False and false are 2 different things in python

bool my_bool = True (Not needed in python)
==> During runtime, python decides the datatype of the variable and allocates the necessary memory.


Operators:
==========

+, -, *, /

first_num = 20
second_num = 10

add = first_num + second_num
sub = first_num - second_num
mul = first_num * second_num
div = first_num / second_num        # Returns a floating point as a result

print(add, sub, mul, div)

print(type(div)) ==> To know the type of datatype.

// -> Integer division
quotient = first_num // second_num
print(quotient, type(quotient))

% -> Modulo operator
remainder = first_num % second_num
print(remainder, type(remainder))

 
String datatype:
----------------- 
A string consists of characters.
sample_str = "Welcome to python session"

A string is an array of characters
--> How to access individual elements in an array?
Python is zero index based, -ve index based